#!/usr/bin/env python

from typing import List

def maxTurbulenceSize(arr: List[int]) -> int:
    n = len(arr)
    ans = 0
    for i in range(n):
        for j in range(i, n):
            if nums[j]  
            ans += 1

    return ans
